version: "3.9"

services:
  todo-app:
    build:
      context: .
      dockerfile: Dockerfile

    image: todo-devops:v1

    container_name: todo-app

    ports:
      - "8080:80"

    restart: unless-stopped